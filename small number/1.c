#include <stdio.h>
int main()
{
int a,c,i;
printf("entre 5 numbers (space in between) :");
for( i=1;i<=5;i++)
{
    scanf("%d",&a);
    if(a<c);
    {
        c=a;
    }
}
 printf("the smallest is %d",c);
return 0;
}